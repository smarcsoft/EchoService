To build the docker container of the server:
--------------------------------------------
docker build -t echoclient .


TODO:
 I need the server jar dependency in this local file system to be able to install it when i build the client in the docker container.
 
