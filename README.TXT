The objective of this project is to deploy on an Azure Kubernetes cluster a docker image containing the grpc server
Go to the scripts directory to deploy and operate everything.
Go to the docker directory to build the containers.
Go to the build directory to build the server (and client) locally.

