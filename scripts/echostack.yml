version: '3.7'

services:
  echoserver:
    image: sebmarc/echoservice:latest
    ports:
      - 50051
    deploy:
      replicas: 2
    networks:
    - echonet


networks:
  echonet:
    external: true
